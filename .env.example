# The main Task 2 pipeline does not require API keys, paid credentials, Docker, or Neo4j.
# Future optional settings can be copied into a local .env file.

WEATHER_KG_CONFIG_DIR=config
WEATHER_KG_LOG_LEVEL=INFO
